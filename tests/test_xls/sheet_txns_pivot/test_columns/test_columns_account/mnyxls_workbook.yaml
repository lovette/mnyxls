workbook:
  worksheets:
    "Category by account category":
      sheet_type: "txns:pivot"
      options:
        rows: "category"
        columns:
          - "account_category"
          - "account_classification"
